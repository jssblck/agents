interface:
  display_name: "Ship It"
  short_description: "Publish a PR with scoped proof and green CI"
  default_prompt: "Use $ship-it to publish this change, add verification evidence, mark the PR ready, and drive CI green."
