interface:
  display_name: "Ship It"
  short_description: "Publish a PR with scoped proof and green CI"
  default_prompt: "Use $ship-it to publish this change as a non-draft PR, then run signoff, add verification evidence, and drive CI green."
