interface:
  display_name: "Merge Open PRs"
  short_description: "Resolve and merge the open PRs one at a time"
  default_prompt: "Use $merge-open-prs to snapshot the open PRs, resolve each onto the default branch, and merge them one at a time."
